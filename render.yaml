services:
  - type: web
    name: car-auction-homepage
    env: node
    buildCommand: cd app && npm install && npm run build
    startCommand: cd app && npm start
    envVars:
      - key: NODE_ENV
        value: production
